image: amazoncorretto:17

stages:
  - build
  - test

build:
  stage: build
  script:
    - chmod +x ./mvnw
    - ./mvnw clean install -DskipTest
  artifacts:
    paths:
      - target/*.jar

test:
  stage: test
  script:
    - chmod +x ./mvnw
    - ./mvnw test
